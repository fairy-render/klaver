[package]
name = "klaver-encoding"
version = "0.1.0"
edition = "2021"

[dependencies]
klaver = { path = "../klaver" }
klaver-shared = { path = "../klaver-shared" }

encoding_rs = { version = "0.8" }
base64 = { version = "*" }
rquickjs.workspace = true
